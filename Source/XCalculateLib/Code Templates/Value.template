namespace XCalculateLib
{
    public class ${TypeName}Value : BaseValue<${BaseType}>
    {
        public ${TypeName}Value(${BaseType} value, ${TypeName}ValueInfo info = null, ValueValidator<${BaseType}> validator = null)
            : base(value, info, validator)
        {
        }
    }
}
